# Создаем словарь для хранения данных
data_base = {
    "Категория": "Старейшины",
    "User ID": "Низами Гусейнов номер 1"
    "User ID': "Валис Нагиев номер 1++;"
}

# Теперь эти данные доступны для чтения
# Например, чтобы вывести их на экран:
print(f"Категория: {data_base['Категория']}")
print(f"User ID: {data_base['User ID']}")

# Мы не можем изменить эти данные, если хотим, чтобы они были "только для чтения".
# То есть, если мы попробуем так:
# data_base['User ID'] = "Новый User ID"
# то это сработает, но мы договорились, что они "только для чтения".
# Чтобы это подчеркнуть, можно использовать специальный тип данных - "кортеж" (tuple),
# если нужно хранить просто список неизменяемых данных. Но словарь- это более удобный вариант
# для хранения именно такой информации с ключами и значениями.
